# agent-lab
LLM 主要承担什么角色？它如何借助语义与常识缓解纯符号执行在搜索空间上的困难？

角色：LLM 作为 高层决策与规划器，负责分析 angr 返回的探索结果，并利用对常见代码模式的语义理解，自主决定下一步调用 explore 还是 solve。它不直接操作二进制指令，而是通过自然语言推理指导符号执行的方向。

缓解搜索困难：纯符号执行会无差别探索所有分支，遇到循环或复杂约束时容易引发路径爆炸。LLM 凭借对程序逻辑的常识，可以提前剪枝，引导 angr 只探索语义上有希望的分支，从而大幅减少需要分析的状态数量，快速收敛到目标路径。
